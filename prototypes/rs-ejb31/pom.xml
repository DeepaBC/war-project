<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" 
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
         <groupId>ejava.build</groupId>
         <artifactId>ejava-project</artifactId>
         <version>1.0-SNAPSHOT</version>
         <relativePath>../../ejava-build/ejava-project</relativePath>
    </parent>

    <groupId>ejava.ws.archetype</groupId>
    <artifactId>rs-ejb31</artifactId>
    <packaging>war</packaging>

    <name>JAX-RS as EJB 3.1 WAR</name>
    <description>
        This project implements JAX-RS application as an EJB 3.1 Sesson
        Bean deployed within a WAR.
    </description>

    <build>
        <finalName>${project.artifactId}</finalName>
    </build>
    
    <dependencies>
        <dependency>
            <groupId>ejava.common</groupId>
            <artifactId>ejava-test-util</artifactId>
            <version>${ejava.version}</version>
            <scope>test</scope>
        </dependency>        
        <dependency>
            <groupId>ejava.common</groupId>
            <artifactId>ejava-it-util</artifactId>
            <version>${ejava.version}</version>
            <scope>test</scope>
        </dependency>        
    </dependencies>
</project>
