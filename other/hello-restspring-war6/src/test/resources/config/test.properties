#the port the server instance will listen on
jetty.port=9092
#${jetty.port}
contextPath=/
resourceBase=src/main/webapp
#the port the client connects to -- different when using a proxy
server.protocol=http
server.port=${jetty.port}
#server.port=9093
server.host=localhost
#appURI=${server.protocol}://${server.host}:${server.port}${contextPath}
