<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
         <groupId>ejava.build</groupId>
         <artifactId>ejava-project</artifactId>
         <version>1.0-SNAPSHOT</version>
         <relativePath>../../ejava-build/ejava-project</relativePath>
    </parent>

    <groupId>ejava.examples.restintro</groupId>
    <artifactId>restintro-dmv-war6</artifactId>
    <packaging>war</packaging>

    <name>DMV REST Introduction</name>
    <description>
        This project implements portions of the DMV example domain
        model as a single JavaEE WAR using JAX-RS in order to
        provide an introductory understanding of how CRUD and 
        RESTful based services look.
    </description>
    
    <properties>
        <servlet.host>${jboss7.host}</servlet.host>
        <servlet.port>${jboss7.port}</servlet.port>
    </properties>

    <build>
        <finalName>${project.artifactId}</finalName>
    </build>
    
    <dependencies>
        <dependency>
            <groupId>ejava.common</groupId>
            <artifactId>ejava-util</artifactId>
            <version>${ejava.version}</version>
            <scope>compile</scope>
        </dependency>        
        <dependency>
            <groupId>ejava.common</groupId>
            <artifactId>ejava-it-util</artifactId>
            <version>${ejava.version}</version>
            <scope>test</scope>
        </dependency>        
         <dependency>
             <groupId>org.apache.httpcomponents</groupId>
             <artifactId>httpclient-cache</artifactId>
            <scope>test</scope>
         </dependency>        
    </dependencies>
</project>
